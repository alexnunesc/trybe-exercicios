const express = require('express');
const fs = require('fs').promises;
const path = require('path');
// const arquivo = './movies.json';

// const main = async () => {
//   try {
//     const ler = await fs.readFile(arquivo, 'utf8');
//     console.log(ler);
//   } catch (error) {
    
//   }
// };

const app = express();

module.exports = app;
